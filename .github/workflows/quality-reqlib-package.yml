name: Quality - Reqlib Package

on:
  workflow_call:

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.ref_name }}
  cancel-in-progress: true

env:
  CICD: 1

jobs:
  test-reqlib-package:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - name: Install uv and set the python version
        uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true
      - name: Install mellea from main branch
        run: uv pip install git+https://github.com/generative-computing/mellea.git@main
      - name: Run Reqlib Package Tests
        run: |
          cd mellea_contribs/reqlib_package
          uv pip install git+https://github.com/generative-computing/mellea.git@main
          uv pip install eyecite citeurl playwright pytest pytest-asyncio pytest-cov
          PYTHONPATH=src python -m pytest -v test/
      - name: Send failure message
        if: failure()
        run: echo "Reqlib package tests failed. Please verify that tests are working locally."
