name: CI - CrewAI Backend

on:
  pull_request:
    paths:
      - 'mellea_contribs/crewai_backend/**'
      - '.github/workflows/ci-crewai-backend.yml'
      - '.github/workflows/quality-generic.yml'
  push:
    branches:
      - main
    paths:
      - 'mellea_contribs/crewai_backend/**'
      - '.github/workflows/ci-crewai-backend.yml'
      - '.github/workflows/quality-generic.yml'

jobs:
  test:
    uses: ./.github/workflows/quality-generic.yml
    with:
      subpackage: mellea_contribs/crewai_backend
      timeout_minutes: 90

